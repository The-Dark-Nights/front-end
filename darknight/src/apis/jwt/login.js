import { GITHUB_AUTH_URL, ACCESS_TOKEN, REFRESH_TOKEN } from './contains';


function Login(props){
    useEffect(() => {
        if(this.props.location.pathname){
            
        }
    },[]);
}
export default Login;